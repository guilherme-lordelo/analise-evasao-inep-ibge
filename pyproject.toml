[project]
name = "analise-evasao-inep-ibge"
version = "0.0.0"
dependencies = [
    "pyyaml",
	"pandas",
	"numpy",
    "matplotlib",
    "geopandas",
    "xlrd",
    "ruamel.yaml",
    "streamlit>=1.30",
]

[tool.setuptools]
package-dir = {"" = "src"}
